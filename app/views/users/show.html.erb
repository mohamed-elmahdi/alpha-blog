<h1 align="center" class="user-title">Welcome to <%= @user.username %>'s page</h1>
<div class="row">
  <div class="col-md-4 ml-md-auto mr-md-auto center">
    <%= gravatar_for @user %>
  </div>
</div>
<h4 align="center" class="user-title"><%= @user.username %>'s articles</h4>
<%= will_paginate @articles%>
<%= render 'articles/form', obj: @articles %>
<%= will_paginate @articles%>